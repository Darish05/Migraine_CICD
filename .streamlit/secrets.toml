# Streamlit Cloud Secrets
# Add these to your Streamlit Cloud app settings
# https://share.streamlit.io/ -> App Settings -> Secrets

# API Configuration
[api]
url = "http://localhost:8000"  # Change to your production API URL

# Optional: Add any API keys or tokens here
# [credentials]
# api_key = "your-api-key"
# auth_token = "your-auth-token"
